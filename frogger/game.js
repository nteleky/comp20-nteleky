/* Comp20 - Frogger Assignment Part 1 * Nicholas Teleky * * Contains all code for rendering graphics. *///Global Variables:var levelNum, highScore, currScore;var frogX, frogY; // X, Y coord for frog.// Function: start_game()// Initializes all gameplay (renders graphics... for now...)function start_game() {        initialize_variables();        draw_static_gameboard();        render_numLives();        render_init_text();        draw_frog();        draw_car();        draw_log();}// Function: initialize_variables()// Initializes global variables for start of gameplay.// **NOTE: After animation is added, more necessary variables will be added to// this function as needed.function initialize_variables() {    levelNum = 1;    highScore = 0;    currScore = 0;    frogX = 180; //sets to init x position    frogY = 490; //sets to init y position}// Function: draw_static_gameboard()// Renders static (unchanging) gameboard graphicsfunction draw_static_gameboard() {    canvas = document.getElementById('game');    if (canvas.getContext) {        ctx = canvas.getContext('2d');        //Draws water and road rectangles        ctx.fillStyle = "#191970";        ctx.fillRect(0, 0, 399, 280);        ctx.fillStyle = "#000000";        ctx.fillRect(0,280, 399, 285);        //Splices FROGGER title        title = new Image();        title.src = 'assets/frogger_sprites.png';        title.onload = function() {        ctx.drawImage(title, 11, 9, 329, 37, 5, 5, 329, 37);}        //Splices grass        grass = new Image();        grass.src = 'assets/frogger_sprites.png';        grass.onload = function() {        ctx.drawImage(grass, 0, 53, 399, 59, 0, 50, 399, 59);}        //Splices purple 'safe zones'        safe = new Image();        safe.src = 'assets/frogger_sprites.png';        safe.onload = function() {        ctx.drawImage(safe, 0, 116, 399, 40, 0, 270, 399, 40);        ctx.drawImage(safe, 0, 116, 399, 40, 0, 480, 399, 40);}    }}// Function: render_numLives()// Renders a frog for each life (note: in future, function will accept a// parameter giving the number of lives remaining and function will loop to// create them).function render_numLives() {    frog = new Image();    frog.src = 'assets/frogger_sprites.png';    frog.onload = function() {    ctx.drawImage(frog, 10, 333, 26, 26, 3, 525, 15, 15);    ctx.drawImage(frog, 10, 333, 26, 26, 19, 525, 15, 15);}}// Function: render_init_text()// Renders high score, level, and score text. function render_init_text() {    write_level();    write_scores();}//Writes the current level to the screen.function write_level() {    ctx.fillStyle = "#00FF00"    ctx.font="bold 20px Arial";    ctx.fillText("Level " + levelNum, 45, 535);}//Writes the current score and high score to the screen.function write_scores() {    ctx.fillStyle = "#00FF00"    ctx.font="bold 14px Arial";    ctx.fillText("Score: " + currScore, 2, 555);    ctx.fillText("High Score: " + highScore, 70, 555);}function draw_frog() {    frog = new Image();    frog.src = 'assets/frogger_sprites.png';    frog.onload = function() {        ctx.drawImage(frog,10, 367, 25, 25, frogX, frogY, 25, 25);    }}function draw_car() {    car = new Image();    car.src = 'assets/frogger_sprites.png';    car.onload = function() {        ctx.drawImage(car, 7, 267, 33, 25, 70, 455, 33, 25);        ctx.drawImage(car, 7, 267, 33, 25, 200, 455, 33, 25);        ctx.drawImage(car, 103, 302, 52, 22, 25, 320, 52, 22);    }}function draw_log() {    log = new Image();    log.src = 'assets/frogger_sprites.png';    log.onload = function() {        ctx.drawImage(log, 6, 194, 120, 25, 100, 180, 120, 25);    }}