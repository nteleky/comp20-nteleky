/* Comp20 - Frogger Assignment Part 1 * Nicholas Teleky * * Contains all code for rendering graphics. */// Function: start_game()// Initializes all gameplay (renders graphics... for now...)function start_game() {        draw_static_gameboard();        render_numLives();        render_text();}// Function: draw_static_gameboard()// Renders static (unchanging) gameboard graphicsfunction draw_static_gameboard() {    canvas = document.getElementById('game');    if (canvas.getContext) {        ctx = canvas.getContext('2d');        //Draws water and road rectangles        ctx.fillStyle = "#191970";        ctx.fillRect(0, 0, 399, 280);        ctx.fillStyle = "#000000";        ctx.fillRect(0,280, 399, 285);        //Splices FROGGER title        title = new Image();        title.src = 'assets/frogger_sprites.png';        ctx.drawImage(title, 11, 9, 329, 37, 5, 5, 329, 37);        //Splices grass        grass = new Image();        grass.src = 'assets/frogger_sprites.png';        ctx.drawImage(grass, 0, 53, 399, 59, 0, 50, 399, 59);        //Splices purple 'safe zones'        safe = new Image();        safe.src = 'assets/frogger_sprites.png';        ctx.drawImage(safe, 0, 116, 399, 40, 0, 270, 399, 40);        ctx.drawImage(safe, 0, 116, 399, 40, 0, 480, 399, 40);    }}// Function: render_numLives()// Renders a frog for each life (note: in future, function will accept a// parameter giving the number of lives remaining and function will loop to// create them).function render_numLives() {    frog = new Image();    frog.src = 'assets/frogger_sprites.png';    ctx.drawImage(frog, 10, 333, 26, 26, 3, 525, 15, 15);    ctx.drawImage(frog, 10, 333, 26, 26, 19, 525, 15, 15);}// Function: render_text()// Renders high score, level, and score text. Note: in future, these may be// split into different functions and given parameters; but for purpose of// this assignment, they will be put into one function.function render_text() {}// drawImage(image, sx, sy, swidth, sheight, x, y, width, height)